# Photography folders

Create one folder per region, for example:

```text
images/photography/
  hong-kong/
  shenzhen/
  mainland-china/
  europe/
```

Export web copies as JPG or WebP at roughly 2000 to 2600 pixels on the long
edge. Add each path and optional caption to `_data/photo_albums.yml`.

